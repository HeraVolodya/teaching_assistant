"""Коди виходу воркера — протокол між воркером і супервізором.

Винесено в окремий листковий модуль навмисно. Раніше константа жила в
`app/worker.py`, і виходив цикл:

    app.worker → app.jobs.runner → app/jobs/__init__.py
               → app.jobs.supervisor → app.worker  (частково ініціалізований)

Цикл не падав лише завдяки щасливому порядку імпортів в API-процесі:
`main.py` першим тягнув `app.jobs.supervisor`, і до звернення по
`RECYCLE_EXIT_CODE` модуль `app.worker` встигав ініціалізуватись. Але
`import app.worker` напряму — саме те, що робить будь-який тест або скрипт —
падав з ImportError. Модуль без жодних внутрішніх залежностей розриває цикл
назавжди.
"""

from __future__ import annotations

__all__ = ["RECYCLE_EXIT_CODE"]

# Воркер просить себе перезапустити: алокатор torch не повертає пам'ять ОС,
# тож після N документів процес тримає гігабайти. Супервізор бачить цей код і
# піднімає новий процес БЕЗ нарощування лічильника невдач — це планова
# переробка, а не збій.
RECYCLE_EXIT_CODE = 75
