"""Скрипти вимірювань НДР.

Це не частина застосунку, а лабораторія: три бейк-офи, цифри з яких ідуть у
звіт і в статтю (Віхи 1, 2 і оцінка швидкості приймання).

    ocr_bakeoff.py    — CER / WER / українські плутанини по рушіях OCR
    table_fidelity.py — поклітинний F1 таблиць по режимах TableFormer
    bench_ingest.py   — хв/стор. у FAST і DEEP, розподіл ConfidenceReport, пам'ять

Пакет навмисно не входить у колесо (`[tool.hatch.build.targets.wheel]
packages = ["app"]`): на машині викладача цим скриптам робити нічого. Але він
є пакетом, а не набором окремих файлів, щоб `python -m scripts.ocr_bakeoff`
працював з кореня `backend/` і щоб чисті функції можна було покрити тестами.

УСІ важкі рушії — ОПЦІЙНІ імпорти. Відсутній рушій пропускається з чесним
повідомленням у консоль і рядком у звіті; він ніколи не валить прогін і
ніколи не підмінюється тихо іншим.
"""

from __future__ import annotations

__all__: list[str] = []
